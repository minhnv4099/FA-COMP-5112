# @package agent.critic

name: Critic
metadata:
  description: This node acts as a critic agent, analyzes, points out flaws in given images and gives fixes

save_rendered_dir: assets/rendered_images/critic
anchor_script_path: assets/blender_script/anchor_critic.py
camera_template_file: templates/camera_templates/template.json
camera_setting_file: src/camera/prepare_cam_data.py

validating_prompt: null
max_critics: 1
n_rendered_images: 4

input_schema:
  type: state
  name: critic
output_schema:
  type: structured_output
  name: critic
tool_schemas: [ ]

edges:
  in_coming: [ ]
  out_going: [ ]

template_file: templates/prompt/critic.yaml

use_model: True

model_name: gpt-4o-mini
model_provider: ~
#model_api_key: <API-KEY>
output_schema_as_tool: True
chat_model: null