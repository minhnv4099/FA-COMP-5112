Each agent uses a particular template for system prompt and human-input prompt

Let's adjust the prompt **EXCEPT** `image_url` field and any `{...}`.
Just change the content represented by '**white**' text